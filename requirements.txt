# Основные библиотеки для работы с глубинным обучением и анализом данных:
torch==2.2.2
torchvision==0.17.2
ultralytics>=8.0.0    # Последняя версия пакета ultralytics для работы с YOLO

# Анализ данных и визуализация
pandas>=1.3.0
matplotlib>=3.5.0

# Логирование экспериментов
tensorboard>=2.10.0

# Версионирование данных с помощью DVC
dvc>=3.59.1

# Инструменты для работы с Kaggle (если планируется скачивание датасетов через Kaggle API)
kaggle>=1.5.12

# (Опционально) Если потребуется работа с удалёнными хранилищами через s3fs или аналогичные инструменты,
# можно добавить их, но учтите, что возможны конфликты зависимостей:
# s3fs==2023.10.0
